version: 2.1

orbs:
  docs: nacelle/docs@dev:master

jobs:
  test:
    docker:
      - image: circleci/golang:1.12
    working_directory: ~/service
    steps:
      - checkout
      - run: go get github.com/mattn/goveralls
      - run:
          name: Run unit tests
          command: |
            go get ./...
            goveralls -covermode=atomic -package=./... -race -service=circle-ci -v
  update_docs:
    docker:
      - image: circleci/golang:1.12
    working_directory: ~/service
    steps:
      - docs/update_docs:
          repo: service

workflows:
  version: 2
  test_and_udpate_docs:
    jobs:
      - test
      - update_docs:
          filters:
            branches:
              only: master
